<UserControl x:Class="ImmenseWall.Views.RulesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="20">
            <TextBlock Text="Firewall Exceptions" FontSize="20" FontWeight="Bold"/>
            <TextBlock Text="Manage applications that are allowed to communicate." Foreground="Gray"/>
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Button Content="Add Rule" Command="{Binding AddRuleCommand}" Margin="0,0,10,0" Padding="10,5"/>
                <Button Content="Edit Rule" Command="{Binding EditRuleCommand}" Margin="0,0,10,0" Padding="10,5"/>
                <Button Content="Delete Rule" Command="{Binding DeleteRuleCommand}" Padding="10,5"/>
            </StackPanel>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Exceptions}" SelectedItem="{Binding SelectedRule}" Margin="10" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header=" " Width="30">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding Icon}" Width="16" Height="16"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Application" Binding="{Binding Name}" Width="250"/>
                <DataGridTextColumn Header="Policy" Binding="{Binding PolicySummary}" Width="200"/>
                <DataGridTextColumn Header="Active For" Binding="{Binding Exception.Timer}" Width="100"/>
                <DataGridTextColumn Header="Creation Date" Binding="{Binding Exception.CreationDate}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
