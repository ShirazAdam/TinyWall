<Window x:Class="pylorak.TinyWall.ProcessesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:pylorak.TinyWall"
        mc:Ignorable="d"
        Title="Running Processes" Height="600" Width="900" ResizeMode="CanResizeWithGrip">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0" Background="#F0F0F0" Padding="10">
            <StackPanel>
                <DockPanel>
                    <Button x:Name="btnRefresh" Content="Refresh" Width="75" Height="25" 
                            DockPanel.Dock="Right" Margin="5,0" Click="BtnRefresh_Click"/>
                    <CheckBox x:Name="chkShowAll" Content="Show All Processes (including system processes)" 
                              VerticalAlignment="Center" Margin="5,0" Checked="ChkShowAll_Checked" 
                              Unchecked="ChkShowAll_Unchecked"/>
                </DockPanel>
            </StackPanel>
        </Border>
        
        <DataGrid x:Name="dataGridProcesses" Grid.Row="1" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="True"
                  CanUserResizeRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="All"
                  RowHeaderWidth="0">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Select" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="60"/>
                <DataGridTextColumn Header="Process Name" Binding="{Binding ProcessName}" Width="150"/>
                <DataGridTextColumn Header="PID" Binding="{Binding PID}" Width="80"/>
                <DataGridTextColumn Header="Memory Usage" Binding="{Binding MemoryUsage}" Width="100"/>
                <DataGridTextColumn Header="CPU Usage" Binding="{Binding CPUUsage}" Width="100"/>
                <DataGridTextColumn Header="Session ID" Binding="{Binding SessionID}" Width="80"/>
                <DataGridTextColumn Header="Priority Class" Binding="{Binding PriorityClass}" Width="120"/>
                <DataGridTextColumn Header="Executable Path" Binding="{Binding ExecutablePath}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <Border Grid.Row="2" Background="#F0F0F0" Padding="10" HorizontalAlignment="Right">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="btnAddSelected" Content="Add Selected" Width="100" Height="25" Margin="5" Click="BtnAddSelected_Click"/>
                <Button x:Name="btnClose" Content="Close" Width="75" Height="25" Margin="5" Click="BtnClose_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>