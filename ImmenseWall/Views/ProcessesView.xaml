<Window x:Class="pylorak.TinyWall.Views.ProcessesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:pylorak.TinyWall.Views"
        mc:Ignorable="d"
        Title="Select Process" 
        Height="600" 
        Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        WindowStyle="ToolWindow">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>

        <Style x:Key="HeaderStyle" TargetType="GridViewColumnHeader">
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search Panel -->
        <Border Grid.Row="0" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                Padding="10,5">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox x:Name="SearchTextBox" 
                         Width="200" 
                         VerticalAlignment="Center"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>
                <Button Content="Search" 
                        Command="{Binding SearchCommand}"
                        Margin="5,0" 
                        Padding="10,2"/>
                <Button Content="Clear" 
                        Command="{Binding ClearSearchCommand}"
                        Padding="10,2"/>
            </StackPanel>
        </Border>

        <!-- Process List -->
        <Grid Grid.Row="1">
            <ListView ItemsSource="{Binding Processes}"
                      SelectedItem="{Binding SelectedProcess}"
                      SelectionMode="{Binding MultiSelect, Converter={StaticResource BoolToSelectionMode}}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" Width="200">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Image Source="{Binding Icon}" 
                                               Width="16" 
                                               Height="16" 
                                               Margin="0,0,5,0"/>
                                        <TextBlock Text="{Binding Name}" 
                                                   VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Services" 
                                        DisplayMemberBinding="{Binding Services}"
                                        Width="150"/>
                        <GridViewColumn Header="Path" 
                                        DisplayMemberBinding="{Binding Path}"
                                        Width="400"/>
                    </GridView>
                </ListView.View>
            </ListView>

            <!-- Loading Overlay -->
            <Border Background="#80000000" 
                    Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Loading processes..." 
                               Foreground="White" 
                               FontSize="14"
                               Margin="0,0,0,10"/>
                    <ProgressBar IsIndeterminate="True" 
                                 Width="200" 
                                 Height="20"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="{Binding Processes.Count, StringFormat='{}{0} processes found'}"/>
            </StatusBarItem>
        </StatusBar>

        <!-- Buttons -->
        <Border Grid.Row="3" 
                Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="OK" 
                        Command="{Binding SelectCommand}"
                        IsDefault="True"
                        Width="75" 
                        Height="23"
                        Margin="0,0,5,0"/>
                <Button Content="Cancel" 
                        Command="{Binding CancelCommand}"
                        IsCancel="True"
                        Width="75" 
                        Height="23"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>