<UserControl x:Class="ImmenseWall.Views.StatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <StackPanel Margin="20">
            <TextBlock Text="{Binding StatusMessage}" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            <TextBlock Text="System is protected." FontSize="16" Foreground="Green" Margin="0,0,0,20"/>
            
            <GroupBox Header="Firewall Mode" Padding="10">
                <StackPanel>
                    <TextBlock Text="Select Operation Mode:" Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding Modes}" SelectedItem="{Binding CurrentMode}" HorizontalAlignment="Left" Width="200"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </Grid>
</UserControl>
