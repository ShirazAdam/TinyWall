<UserControl x:Class="ImmenseWall.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="20">
            <TextBlock Text="Settings" FontSize="20" FontWeight="Bold"/>
            <TextBlock Text="Configure application behavior." Foreground="Gray"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1">
            <StackPanel Margin="20">
                <GroupBox Header="General" Padding="10" Margin="0,0,0,10">
                    <StackPanel>
                         <CheckBox Content="Automatically check for updates" IsChecked="{Binding AutoUpdateCheck}" Margin="0,0,0,5"/>
                         <CheckBox Content="Allow communication within local subnet" IsChecked="{Binding AllowLocalSubnet}" Margin="0,0,0,5"/>
                         <CheckBox Content="Block network traffic when display is off" IsChecked="{Binding DisplayOffBlock}" Margin="0,0,0,5"/>
                         <CheckBox Content="Prevent changes to Hosts file" IsChecked="{Binding LockHostsFile}" Margin="0,0,0,5"/>
                    </StackPanel>
                </GroupBox>

                 <GroupBox Header="Blocklists" Padding="10">
                    <StackPanel>
                         <CheckBox Content="Enable Blocklists" IsChecked="{Binding EnableBlocklists}" Margin="0,0,0,5"/>
                         <CheckBox Content="Enable Port Blocklist" IsChecked="{Binding EnablePortBlocklist}" Margin="20,0,0,5" IsEnabled="{Binding EnableBlocklists}"/>
                         <CheckBox Content="Enable Hosts Blocklist" IsChecked="{Binding EnableHostsBlocklist}" Margin="20,0,0,5" IsEnabled="{Binding EnableBlocklists}"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
