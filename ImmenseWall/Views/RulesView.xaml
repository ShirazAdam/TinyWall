<UserControl x:Class="ImmenseWall.Views.RulesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="20">
            <TextBlock Text="Firewall Exceptions" FontSize="20" FontWeight="Bold"/>
            <TextBlock Text="Manage applications that are allowed to communicate." Foreground="Gray"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Exceptions}" Margin="10" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" SelectionMode="Single">
            <DataGrid.Columns>
                <!-- Display name (Subject.ToString()) -->
                <DataGridTextColumn Header="Application" Binding="{Binding Subject}" Width="250"/>
                <!-- We can try to bind specific properties if we know them, but Subject is polymorphic. 
                     For now, let's just show basic info. 
                     Maybe Policy? -->
                <DataGridTextColumn Header="Policy" Binding="{Binding Policy}" Width="150"/>
                <DataGridTextColumn Header="Active For" Binding="{Binding Timer}" Width="100"/>
                <DataGridTextColumn Header="Creation Date" Binding="{Binding CreationDate}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
